# =====================================================================
# Docker-Proxy 环境变量示例
# 用法: cp .env.example .env   (docker compose 会自动读取同目录下的 .env)
# 一键部署脚本运行时会自动生成 .env 并写入随机管理令牌, 本文件仅作参考模板。
# =====================================================================

# --- Go 代理管理 API 令牌 ---
# HubCMD-UI 面板调用 go-proxy 管理接口(端口 5001)时校验此令牌, 务必与管理端一致。
# 留空(或此文件不存在)时, 一键部署脚本会自动生成随机令牌并写入 .env。
# 手动填写请使用足够随机的字符串, 切勿使用默认值上线。
GO_PROXY_ADMIN_TOKEN=change-me-to-a-strong-random-token

# --- Hubcmd UI 会话签名密钥 ---
# 用于签名后台登录会话 cookie。请使用 `openssl rand -hex 32` 生成并长期保存；
# 修改此值会让现有登录会话全部失效。
SESSION_SECRET=change-me-to-a-strong-random-secret

# --- 镜像地址 (可选覆盖) ---
# 等号右侧为默认值; 在 .env 中留空或注释掉即使用默认值。
REGISTRY_IMAGE=dqzboy/registry:latest
UI_IMAGE=dqzboy/hubcmd-ui:latest

# 主机显示名称、Secure Cookie 和 Registry 性能参数，
# 请登录后台后在「系统参数」页面管理；保存后会直接热加载，无需修改此文件。
# HTTP_PROXY、HTTPS_PROXY、NO_PROXY 属于部署层配置，在 docker-compose.yaml 中默认注释；
# 一键安装脚本配置上游代理时，会同时为 go-proxy 与 hubcmd-ui 写入并启用。
